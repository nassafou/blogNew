{% extends "BlogNewBundle::layout.html.twig" %}
{% block title %}
    Lecture d'un article - {{ parent() }}
{% endblock %}
{% block yatblog_body %}
<h2>{{ article.titre }}</h2>
<i> Par {{ article.auteur }}, le {{ article.date|date('d/m/Y') }}</i>
<div class="well">
{{ article.auteur }}
</div>
<p>
    <a href="{{ path('yatblog_accueil')}}" class="btn">
        <i class="icon-chevron-left"></i>
        Retour à la liste
    </a>
    <a href="{{ path('yatblog_modifier', {'id': article.id })}}">
        <i class="icon-edit"></i>
        Modifier
    </a>
    <a href="{{ path('yatblog_supprimer', {'id': article.id }) }}">
        <i class="icon-trash"></i>
        Supprimer l'article
    </a>
</p>

{% endblock %}